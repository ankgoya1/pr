name: CI/CD Workflow

on:
  push:
    branches:
      - main

jobs:
  ci-cd:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Call the checkout job directly
      - name: Checkout
        run: |
          # Run the checkout steps here directly
          echo "Checking out code..."

      # Call the build job directly
      - name: Build
        run: |
          # Run the build steps here directly
          echo "Building your project..."

      # Call the publish job directly
      - name: Publish
        run: |
          # Run the publish steps here directly
          echo "Publishing the Artifacts..."
