name: Publish Artifact Workflow

on:
  workflow_call:
    secrets:
      token:
        required: true
      pass:
        required: true
    inputs:    
      user:
        description: Flag indicating whether the apps should be signed
        type: string  
        
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: |
          echo "value is ${{ secrets.token }}"
          echo "This pipeline has been trigger by ${{ inputs.user }}"
          echo "cloning repo....."
          git clone https://ankgoya1:${{ secrets.pass }}@github.com/ankgoya1/demorepo.git
          cd demorepo
          ls -latr
